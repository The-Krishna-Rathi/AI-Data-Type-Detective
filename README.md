# AI-Data-Type-Detective
An agentic pipeline that inspects a dataset, infers each column’s semantic data type, generates a safe conversion plan, executes it with Pandas, validates results, and produces a cleaned dataset + an auditable JSON report.
